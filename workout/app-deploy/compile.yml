---
platform: linux

image_resource:
  type: docker-image
  source: {repository: hedlund/gradlew}

inputs:
- name: compile-scripts
- name: resource-web-app

run:
  path: compile-scripts/workout/app-deploy/compile.sh